<% if @feed_items.any? %>
  <ol class="microposts">
    <%= render @feed_items %> <!-- Rails knows to call the _micropost partial because the @feed_items holds a list of Micropost objects. -->
  </ol>
  <%= will_paginate @feed_items, params: { controller: :static_pages, action: :home } %>
<% end %>